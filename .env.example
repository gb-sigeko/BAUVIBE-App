# Supabase / PostgreSQL (erforderlich)
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/postgres?sslmode=require"

# Optional: separate Postgres/Supabase-DB für automatisierte Tests (sonst DATABASE_URL)
# TEST_DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/postgres?schema=bauvibe_test&sslmode=require"

# Playwright / E2E (optional)
# E2E_BASE_URL="http://127.0.0.1:3000"
# E2E_EMAIL="fee@bauvibe.local"
# E2E_PASSWORD="Bauvibe2026!"
# PLANUNG_PERF_LOAD_MS="8000"

# NextAuth (erforderlich)
AUTH_SECRET="mindestens-32-zeichen-langer-zufaelliger-string"

# Optional: E-Mail-Versand (SMTP)
# SMTP_HOST=
# SMTP_PORT=
# SMTP_USER=
# SMTP_PASS=
